node_modules
dist
coverage/
.claude/settings.local.json
/generated/prisma
.vscode

# Environment variables
.env
.env.local
.env.*.local

# Test fixtures — stamp de regeneração do schema snapshot
tests/fixtures/*.stamp

# Load test (Card #229) — JWTs efêmeros do pool, nunca versionar (defesa em
# profundidade sobre loadtest/.gitignore, cobre qualquer CWD).
**/.jwts.json
